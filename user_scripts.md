# Richping 단계별 Codex 요청문

작성일: 2026-09-20. [ROADMAP.md](ROADMAP.md)의 R0~R6 실행용 프롬프트 모음이다. 이 파일은 새 요구사항이나 구현 완료 기록이 아니다. 현재 계약과 진행 상태는 `PROJECT_SPEC.md`, `docs/DESIGN.md`, `PROJECT_STATUS.md`를 따른다.

## 사용 방법

1. `C:\richping`을 작업 폴더로 열고 아래 표의 모델·추론 등급을 선택한다. **등급은 구독 요금제나 서비스 우선순위가 아니라 reasoning effort다.** 프롬프트 안에 모델 이름을 적는 것만으로 실행 모델이 바뀐다고 가정하지 않는다.
2. 처음에는 **R0-A 코드 블록 하나만 복사해서 요청**한다. 각 블록은 이 파일의 공통 규칙을 읽도록 되어 있어 공통 문장을 매번 함께 복사할 필요가 없다.
3. 구현 완료 후 지정된 다음 요청을 사용한다. 미래 관찰 대기는 구현 실패가 아니다. 모든 단계를 한 번에 요청하거나, 미래 표본을 과거 재생으로 대신 채우지 않는다.
4. 각 세션은 실제 Git/코드/로컬 산출물을 다시 확인한다. 이미 끝난 작업은 반복하지 않고 남은 범위만 처리한다. 초기 테스트 개수나 commit/model ID를 영구 기준으로 고정하지 않는다.
5. R0 운영 시작 이후 R1과 좁은 R3 연구는 forward 관찰과 병행 가능하다. 여기서 병행은 작업 경로 분리를 뜻하며 다중 에이전트 실행을 요구하지 않는다. 복잡한 에이전트 계층을 추가하지 않는다.
6. 읽기 전용 검토만 요청한 블록에서는 코드를 수정하지 않는다. 구현 블록은 승인된 범위를 구현·검증·문서 반영까지 완료하고 단순 계획으로 끝내지 않는다.

## 추천 모델·등급

아래 배정은 작업 위험과 복잡도를 고려한 **프로젝트용 권장 판단**이며 모델별 Richping 벤치마크 결과는 아니다. 공식 문서는 Astra를 복잡한 다단계 작업, Sol을 깊은 분석과 복잡한 구현, Terra를 일상 작업에 적합한 선택으로 설명하며, 어려운 작업에 High/Extra High를 사용할 수 있다고 안내한다. 사용 가능 모델과 UI 설정은 계정·클라이언트에 따라 다르다. [OpenAI 공식 모델 안내](https://learn.chatgpt.com/docs/models)

High=`high`, Extra High=`xhigh`, Medium=`medium`. 구체 설정이 보이면 이를 선택한다. UI에 해당 설정이 없으면 사용 가능한 인접 설정을 선택하고 실제 사용 모델·등급을 기록한다. 이 작업들을 위해 기본적으로 Max/Ultra를 사용할 필요는 없다. Astra를 사용할 수 없으면 Sol High로 진행하되 복잡한 계약 판정은 별도 검수와 미확인 항목을 남긴다. 모델을 바꿨다는 사실 자체가 검증을 대신하지 않는다.

| 요청 | 수행 범위 | 추천 모델 | 등급 | 시작/다음 조건 |
|---|---|---|---|---|
| R0-A | 일일 관찰 제품 구현 | GPT-5.6 Sol | High | 지금 시작 → R0-B |
| R0-B | 실제 운영 시작·스케줄러 확인 | GPT-5.6 Sol | High | R0-A 개발 완료 → R1-A |
| R1-A | baseline 평가 및 paper 계약 확정 | GPT-6 Astra | High | R0 버전/관측 계약 마련 → R1-B |
| R1-B | 최소 paper 원장 구현·검증 | GPT-5.6 Sol | High | R1-A 계약 작성 → 검수 Q |
| R2 | universe 관찰 pilot | GPT-5.6 Sol | High | R0 연구/운영 격리 후, 비용·공급 계약 확인 |
| R3-A | baseline 포착 진단·가설 1개 등록 | GPT-6 Astra | High | 좁은 연구는 R0 이후, 넓은 연구는 R2 이후 |
| R3-B | 제한 challenger 구현·연구 평가 | GPT-5.6 Sol | High | R3-A 실험 명세 고정 → 검수 Q |
| R4-A | 추가 시장 데이터 필요성 판정 | GPT-6 Astra | High | R1/R3에서 구체적 오류 확인 시에만 |
| R4-B | 승인된 데이터 1종 ablation | GPT-5.6 Sol | High | R4-A GO와 공급/PIT 계약 충족 시에만 |
| R5-A | 승격 증거·정책 계약 설계 | GPT-6 Astra | Extra High | R1/R3 평가 체계 확보 → R5-B |
| R5-B | gate 연결·제한 활성화/강등 구현 | GPT-5.6 Sol | High | R5-A 계약 고정 → 검수 Q |
| R6-A | 운영 안정성·실체결 관찰 기반 | GPT-5.6 Sol | High | R0/R1 후; R5 전체 완성 불필요 |
| R6-B | 소액 실전 활용 가능성 판정 | GPT-6 Astra | Extra High | 실제 증거·운영·위험 자료 확보 시 |
| Q | 핵심 변경 독립 검수 | GPT-6 Astra | High | 회계/PIT/승격 등 중요한 변경 후 |
| F | 검수 finding 수정 | GPT-5.6 Sol | High | 수정할 finding이 있는 경우만 |
| O | 정기 운영·증거 점검 | GPT-5.6 Terra | Medium | 자동 운영 중 주기적/장애 후 점검 |

모든 사소한 변경에 설계·구현·검수 세 세션을 강제하지 않는다. R0는 기존 상세 명세를 바로 구현하고, 새 회계·승격 계약은 분리해서 검토한다. Extra High는 여러 계약 간 충돌을 깊게 검토해야 하는 R5-A/R6-B에 한정한 기본 권장이다.

## 공통 실행 규칙

아래 규칙은 각 요청문에서 명시적으로 참조한다.

- `AGENTS.md`, `PROJECT_SPEC.md`, `PROJECT_STATUS.md`, `ROADMAP.md`, `docs/DESIGN.md`를 읽고 해당 요청의 현재 상태·선행조건을 실제 코드/산출물과 대조한다. 과거 완료 기록은 보존하고 앞으로의 계획을 완료로 쓰지 않는다.
- 작업 시작 시 Git/로컬 dirty 상태를 확인한다. 기존 사용자 변경을 보존하고 맡은 범위만 수정한다. 커밋/푸시는 별도 요청 없이는 하지 않는다. 참조 프로젝트는 read-only다.
- Python modular monolith/SQLite/CLI와 기존 데이터·검증 함수를 우선 재사용한다. 서비스 분리, 복잡한 UI, agent hierarchy, 새 범용 framework를 선행하지 않는다.
- immutable dataset/snapshot/확정 outcome, 시점 cutoff/known_at/label_end, 평가 제외·미해결 분모를 보존한다. Feature 배당 정규화와 outcome 배당 회계는 별개다. 기존 v3/평가 정책을 몰래 완화하거나 과거 결과를 재작성하지 않는다.
- 연구 DB/산출물과 운영 추천·위험 상태의 쓰기를 분리한다. synthetic/research OOS/fresh shadow/paper/실체결을 섞지 않는다. 추천 평균/합계·cohort drawdown을 계좌 성과/MDD로 표시하지 않는다.
- 전체 Python code_hash 변경이 model_id와 위험 상태 연속성에 미치는 영향을 확인한다. 새 모델 ID를 이용해 성과 PAUSED를 초기화하지 않는다.
- 계약 설계 블록은 최소 명세와 결정 근거를 작성한다. 사전 기준을 정하기 전에 확인 OOS를 열람하지 않는다. 구현 블록에서 중대한 미정 계약이 발견되면 관련 작업만 보류하고 독립적으로 가능한 범위는 완료한다. 불필요한 확인 질문으로 멈추지 않는다.
- 실행 코드 변경 시 핵심 불변조건과 관련 회귀를 테스트하고 프로젝트 전체 pytest, CLI help, diff 검사를 수행한다. 기존 artifact를 덮는 기본 demo/daily/validate를 검증 목적으로 무심코 실행하지 않는다. 테스트/실험은 별도 DB·출력 경로를 쓴다. 문서만 바뀌면 문서 정합성/링크/diff 검사로 충분하다.
- 결과가 부정적이거나 증거가 부족해도 threshold를 낮춰 통과시키지 않는다. 실제 미래 기간·시장 국면·사용자 예산·공급자 권한은 합성하거나 추정해 완료 처리하지 않는다.
- 자동 주문은 제외한다. 외부 알림은 수신처와 명시적 발송 지시가 있을 때만 보낸다. 유료 계약·실전 자본·손실 예산을 임의로 결정하지 않는다.
- 마지막에 변경 파일, 검증 결과, 사용자 효용, 남은 제약, `개발 완료 / 운영 확인 / 미래 증거 대기`를 구분해서 보고한다. 상태는 PROJECT_STATUS에 간결하게 남기고 다음에 사용할 요청 ID를 하나 추천한다. 단계를 자동으로 연속 실행하지 않는다.

## R0-A — 일일 관찰 보고서 구현

**추천: GPT-5.6 Sol / High.** 지금 가장 먼저 사용할 요청이다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R0-A를 구현하라.
docs/FIRST_MILESTONE.md가 이 작업의 상세 명세다. 현재 Git/코드/로컬 자료를 확인한 뒤 이미 완료된 부분은 재사용하라.

일일 shadow 보고서에 최신성, 실행 상태, 정상 NO TRADE와 데이터 장애, 후보 근거·ATR 참고가격·기대값의 증거 수준을 명확히 표시하라. 실제 shadow의 관측 요약과 수집 전 실패까지 포함한 실행 시도 기록을 제공하라. 로컬 Markdown/텍스트와 JSON을 우선하고 운영/연구 출력 경로를 분리하라.

기존 baseline의 signal/score/calibration/비용/threshold/universe/top-k와 feature/outcome/evaluation 계약을 변경하지 말라. model ID 변경의 provenance는 명시하되 투자 판단 동일성 및 위험 latch 연속성을 검증하라.

성공, NO TRADE, 수집 실패, stale 보고서, 휴일, 늦은 실행, 중복 실행, PENDING 만기, 원자적 파일 교체 실패를 검증하라. 기존 immutable 데이터와 보고서를 보존하라.

FIRST_MILESTONE의 개발 인수 조건까지 구현·검증하고 보고 예시를 제시하라. 이번 요청은 자동 주문·새 전략·Telegram 발송이나 OS 스케줄러 등록을 포함하지 않는다. 실제 무인 운영 시작 확인은 R0-B로 분리하고, 구현만으로 장기 안정성이나 알파 완료를 선언하지 말라.
```

## R0-B — 실제 forward 운영 시작

**추천: GPT-5.6 Sol / High.** R0-A 개발 인수 통과 후 사용한다. 실행 시간이 맞지 않으면 안전한 준비까지만 하고 실제 관찰은 대기로 남긴다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R0-B를 수행하라.
R0-A의 구현·테스트 결과와 docs/FIRST_MILESTONE.md를 확인한 뒤 실제 forward 운영을 시작할 준비와 검증을 진행하라.

운영 DB, 이전 risk_state, 모델/설정/코드 버전, 최신 dataset의 capture·feature 단위 증거를 확인하라. 기존 자료를 덮지 않고 현재 adapter의 정상 수집 경로로 새 immutable vintage를 확보하라. 보고 기능 변경 때문에 PAUSED를 회피하지 말라.

실제 시각이 신호일 종료 후 다음 개장 전일 때 daily를 실행하고 결과/NO TRADE·로그·보고서 freshness를 확인하라. 시간이 맞지 않으면 backdate하지 말고 가능한 준비를 완료한 뒤 실행 가능한 다음 시점을 기록하라.

기존 Richping Windows 예약 작업을 확인하라. 나는 현재 사용자 권한으로 매일 Asia/Seoul 오전 08:00에 프로젝트 daily wrapper를 실행하는 Richping 전용 작업의 등록을 요청한다. 같은 역할의 작업이 있으면 중복 생성하지 말고 경로·시간대·종료 코드·실행 계정을 확인하라. 관리자 권한이나 OS 보안 정책 완화는 임의로 추가하지 말고, 도구가 요구하는 승인은 정상 절차로 요청하라. 다른 예약 작업을 변경하지 말라.

자동 연구나 외부 메시지 전송은 시작하지 말라. 운영 시작 manifest와 보고 예시, 스케줄러 확인 결과, 간단한 실패 복구 절차를 남겨라. 실제 실행 확인과 미래 다일 안정성 미검증을 구분하고 다음 R1-A로 인계하라.
```

## R1-A — baseline 평가 정리와 paper 회계 계약

**추천: GPT-6 Astra / High.** 코드를 크게 만들기 전에 평가 의미와 자본 계약을 고정한다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R1-A를 수행하라. 이번 범위는 read-only 분석과 최소 paper 계약 문서 작성이며 실행 코드는 수정하지 말라.

현재 baseline의 coverage, frozen OOS, 실패 분석, fresh shadow를 실제 존재하는 자료로 구분하라. 기존 157=122+23+12 같은 역사 분모를 해당 artifact가 존재할 때 검증하고, 없으면 재현 미확인이라고 기록하라. PENDING의 fold 종료 cutoff와 후속 만기 관측, 미해결 배당/결측, SPY unpaired, 운영 위험 latch와 fixed replay의 차이를 정리하라.

기존 계약을 바꾸지 않는 maturity follow-up과 최소 paper ledger의 구현 계약을 작성하라. 진입은 next-open, 기본 청산은 5번째 session close이며 stop/target 참고가격을 실제 체결 수익으로 바꾸지 말라.

가상 초기 자본, sizing, 종목/총 노출·동시 보유 한도, 현금 부족, 중복 추천, 진입/청산 순서, 정수 주식 수, 갭/비용/미체결, 실현·미실현 손익, 기업행동·상폐·미확정 NAV 처리 규칙을 명시하라. 가상 기준값은 단순한 연구 가정으로 선정하고 사용자 실전 예산처럼 취급하지 말라. 결과를 본 뒤 규칙을 최적화하지 말라.

동일 초기 자본·기간 SPY buy-and-hold와 동일 자본 배분·현금 대기 SPY 대체 포트폴리오를 구분하라. 배당 총수익 회계를 검증할 수 없으면 비교 불완전으로 표시하는 계약을 포함하라.

기존 모듈 재사용, 최소 저장 구조, 보존/호환성, 손계산 fixture 및 완료/보류 기준을 짧은 R1 구현 명세로 남겨라. Alpha 판정과 개발 완료를 분리하고 R1-B가 바로 구현할 수 있게 인계하라.
```

## R1-B — 최소 모의 포트폴리오 구현

**추천: GPT-5.6 Sol / High.** 선행: R1-A의 구현 가능한 계약.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R1-B를 구현하라. R1-A가 작성한 paper/평가 명세를 실제로 찾아 읽고 그 범위만 구현하라. 명세가 없거나 핵심 회계 규칙이 미정이면 해당 blocker를 먼저 명시하라.

기존 validation/evaluation/observe를 재사용하여 원본 OOS와 분리된 maturity follow-up, 전체 분모·제외·SPY pairing 진단을 제공하라. 별도 paper 원장으로 자본, 포지션, 현금, 가상 fills, 일별 NAV, 실현·미실현 손익을 추적하라.

중첩 포지션, 같은 종목 재추천, 현금 부족, 거래 순서, 비용·갭·미체결 및 기업행동 미해결을 계약대로 처리하라. 알 수 없는 가치를 0%나 확정 NAV로 바꾸지 말라. 추천 진단과 계좌 지표를 분리하고 두 SPY 비교의 비용/현금/배당 가정을 명시하라.

손계산 fixture로 현금·포지션 보존과 MDD를 검증하고 동일 입력 재실행의 멱등성, 미래 누출, 원본 보존을 확인하라. 연구 재생 paper와 실제 미래 paper를 구분하라. 성과를 좋게 만들기 위한 전략/threshold 변경은 금지한다.

사용자가 가상 자본의 보유·현금·수익·위험과 미확정 부분을 읽을 수 있는 보고서를 완성하라. 회계 개발 완료와 실제 수익성 증거 대기를 별도로 보고하고 핵심 회계 변경은 검수 Q로 인계하라.
```

## R2 — 투자 후보군과 비신호 관찰 범위 확대

**추천: GPT-5.6 Sol / High.** R0 격리 구조가 필요하며 R1의 수익성 결론은 기다리지 않는다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R2의 단계적 universe 관찰 pilot을 수행하라.

먼저 공급자의 목록·과거 membership·상폐·symbol 변경·요청 제한·비용 계약을 공식 자료와 현재 adapter에서 확인하라. 전시장 full-history 수집보다 넓은 목록의 저비용 screening과 좁은 상세 관찰을 우선하라. 공급자가 지원하지 않는 PIT 이력을 있다고 가정하지 말라.

당시 이용 가능한 유동성 기준과 membership 유효시각/known_at, 반복 입출입·신규 상장·거래정지·상폐 식별 계약을 정의하고 지원 가능한 최소 범위를 구현하라. schema 변경 시 기존 immutable 데이터와 읽기 호환성을 검증하라. 과거 PIT가 없으면 시작 이후 forward membership을 수집하고 과거 결과는 research로 제한하라.

10→50→100 종목 규모를 단계적으로 확인하되 각 단계의 수집 시간·메모리·DB 증가·요청 제한과 보고 deadline을 측정한 뒤에만 확대하라. 공급/비용 조건이 안 맞으면 가능한 규모에서 멈추고 이유를 남겨라. 유료 구독은 임의로 구매하지 말라.

신호 종목뿐 아니라 비상승·실패·탈락·결측 종목을 포함한 관찰 패널과 분모를 보존하라. 표본을 줄이면 추출 규칙/비율을 남겨라. 증분 수집·캐시·재시도·재개를 검증하라.

이번 변경은 연구 관찰 후보군의 확대이며 운영 baseline 8종목을 자동 교체하지 않는다. 미래 자료가 과거 membership을 바꾸지 않는 테스트와 비용/coverage 보고서를 완성하라.
```

## R3-A — 패턴 포착 진단과 가설 하나 선택

**추천: GPT-6 Astra / High.** 좁은 baseline 연구는 R0 이후 시작 가능하다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R3-A를 수행하라. 분석 및 실험 사전등록 문서만 작성하고 새 전략 코드는 구현하지 말라.

현재 관찰 범위에서 baseline이 급등 전 특징, 추세 지속, 눌림목·재진입, RS 개선/돌파 중 어떤 기회를 언제 포착하고 놓쳤는지 평가하라. 신호가 없는 종목·비상승 유사군·실패 사례를 포함하라. 넓은 시장 자료가 없으면 8종목 연구의 한계를 표시하라.

CRCL의 사후 저점/특정 가격이나 고수익 종목을 정답으로 맞추지 말라. trend 구간은 당시까지 관측 가능한 기간/고점 거리/MA 등으로 정의하고 미래 상승 label과 feature를 분리하라. 기존 failure-analysis의 ticker-conditioned calibration 제안도 하나의 후보일 뿐 자동 채택하지 말라.

연구 가치와 데이터 가능성을 비교해 가설 최대 1개, 변경축 최대 1개를 선택하라. 명확한 근거가 없으면 무채택으로 종료할 수 있다. 선택 시 사전 조건·실패 조건·진입/청산·horizon·비용·대조군·주지표·trial budget·다중시험 처리·중단 기준을 고정하라.

discovery/validation/확인 OOS/fresh forward를 분리하고 이미 읽은 M2-1B OOS는 discovery로 표시하라. 확인용 구간은 결과를 열람하지 않고 사용 가능성과 오염 이력부터 확인하라. 없으면 탐색 구현은 가능하되 성능 입증은 대기로 남겨라.

기존 experiments 재사용과 실패 trial 기록, 후보/Champion 동일 조건 비교, R3-B 구현 범위를 최소 실험 명세로 남겨라. 결과가 좋아야만 작업 성공이라고 정의하지 말라.
```

## R3-B — 제한 challenger 연구 구현

**추천: GPT-5.6 Sol / High.** 선행: 고정된 R3-A 가설·실험 명세.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R3-B를 구현하라. R3-A의 사전등록 명세를 찾아 읽고 가설 1개·변경축 1개 범위만 수행하라. 무채택 판정이면 후보를 임의로 만들지 말라.

운영 Champion과 분리된 strategy/model ID 및 연구 DB에서 challenger를 실행하라. Engine/observe/validation/evaluation을 재사용하고 trial을 실행 전에 등록하며 실패·재시도·폐기를 포함한 family 이력을 남겨라.

미래 feature/label leakage, label_end·purge/embargo, 동일 날짜·중첩 horizon 상관, 비용 stress, nonwinner 대조군, fold/ticker/regime 집중도를 검증하라. Champion은 동일 frozen 입력의 투자 판단을 유지하고 저장된 원본 byte를 보존하라. code hash 변화는 별도 provenance로 설명하라.

이미 본 OOS의 재평가는 discovery 진단으로만 보고하라. 확인 OOS가 있으면 사전 규칙에 따라 한 번 평가하고 소비 이력을 남겨라. 없다면 연구 결과와 개발 완료만 보고하고 미사용 OOS/fresh 증거를 기다려라.

추천 빈도를 만들기 위한 threshold 완화나 사후 파라미터 재탐색은 하지 말라. 운영 추천/위험 상태에는 쓰지 말고 자동 승격하지 말라. 개선·실패·불충분 중 실제 결과를 비교 카드로 남기고 검수 Q로 인계하라.
```

## R4-A — 시장 환경 데이터 추가 여부 결정

**추천: GPT-6 Astra / High.** 선택 단계이며, 오류 근거가 없으면 건너뛴다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R4-A를 수행하라. 실행 코드는 수정하지 말라.

R1/R3의 실제 결과에서 현재 SPY/QQQ 추세·SPY 변동성 모델이 실패하는 구체적 상황을 찾아라. 그 오류를 줄이는 데 가격 기반 시장 폭/섹터 RS, VIX, 금리·채권, 경제 발표 중 무엇이 필요한지 비교하라. 설명력이 있어 보인다는 이유만으로 전부 추가하지 말라.

효용 가능성·PIT/발표 vintage 가용성·수집비용·coverage 손실·유지보수를 기준으로 데이터 최대 1종의 GO/DEFER/DROP을 결정하라. 공급 조건은 당시 공식 자료로 확인하라. 옵션·뉴스 NLP를 선행 확장하지 말라.

GO이면 observation/released/revised/ingested/known_at, availability lag, missing/stale 동작, baseline-only 대비 ablation, 동일 split/비용/주지표와 미사용 평가 구간을 사전 등록하라. 경제지표 최종 수정값의 역사 소급을 금지하라.

R4-B가 구현할 최소 명세와 보류 기준을 남겨라. 개선 근거 또는 적격 데이터가 없으면 보류 판정으로 이 작업을 종료하고 다른 유용한 다음 단계를 제시하라.
```

## R4-B — 추가 데이터 한 종류의 효과 검증

**추천: GPT-5.6 Sol / High.** R4-A GO일 때만 사용한다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R4-B를 구현하라. R4-A의 GO 판정·데이터 계약·사전등록 평가 명세를 확인하라. 조건이 충족되지 않으면 임의 데이터로 대체하지 말라.

승인된 데이터 한 종류만 연구 경로에 추가하고 발표/수정/수집 시점 및 vintage를 보존하라. 기존 정보만 사용하는 baseline과 동일 기간·비용·표본 조건의 ablation을 수행하라. 확인 OOS는 사용 이력을 남기고 재튜닝에 반복 사용하지 말라.

미공개 release와 미래 revision 차단, missing/stale fallback, 기존 baseline 재현과 운영 격리를 테스트하라. 개선값뿐 아니라 거래 빈도·coverage 손실·불확실성·비용·tail 변화를 보고하라.

성과 개선이 없으면 미채택으로 종료하라. 개선이 있어도 R5 증거 절차 없이 운영 추천에 반영하지 말라. 데이터/연구 구현 완료와 실제 성능 입증 수준을 구별해 남겨라.
```

## R5-A — 승격·강등 증거 계약 설계

**추천: GPT-6 Astra / Extra High.** 통계·시점·운영 권한이 만나는 단계다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R5-A를 수행하라. 실행 코드 변경 없이 증거 builder·승격/강등 정책의 최소 구현 명세를 작성하라.

현재 promotion_gate, experiments, 실제 R1/R3 결과를 대조하라. 빈 evidence 호출과 실제 측정 통과/실패/미측정을 구분하고, 각 gate 필드의 출처·분모·기간·버전·완전성을 명시하라.

기존 최소 OOS 200건/60 signal dates/3 folds, PF≥1.1, 비용 stress 양수, paired 개선 CI 하한>0 및 PIT/완전성/안정성/다중시험/fresh 조건을 완화하지 말라. ROADMAP의 fresh 관찰 정책안은 결과 열람 전에 확정할 별도 정책으로 다루며 코드에 이미 구현된 규칙처럼 쓰지 말라. portfolio 자본·tail/MDD 기준과 관측 중 반복 검정 통제도 명시하라.

holdout 소비 원장, 실패 포함 trial family, evidence와 model/data/config ID 연결, 미측정·오래된·오염된 증거 거절을 설계하라. Research가 Production에 직접 쓰지 못하게 하라.

shadow review 적격→fresh 관찰→제한 활성화→추가 관찰의 상태 전이와 유효시각, 강등/중단/검증된 대안 복귀를 정의하라. 성과 latch를 임의 초기화하지 말라. 대안이 없으면 현금/NO TRADE가 정상이다.

개발로 해결할 infrastructure와 실제 미래 증거를 구분하라. 현재 데이터로 통과가 불가능하면 그 상태로 gate를 구현할 수 있게 설계하라. 자동 주문은 제외하고 R5-B 인수 테스트와 호환성 영향을 명시하라.
```

## R5-B — 실제 증거에 연결된 gate와 전략 전환

**추천: GPT-5.6 Sol / High.** 정책 구현과 전략 실제 활성화는 별도 판정이다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R5-B를 구현하라. R5-A의 고정 정책을 읽고 evidence builder, holdout 사용 이력, 승격/강등 상태 전이를 기존 엔진 위에 최소 구조로 추가하라.

실제 저장 증거로 gate를 계산하고 pass/fail/unmeasured를 구분하라. 서로 다른 비용·universe·기간·버전 혼합, 잘못된 ID, 누락/부적격/오염된 결과를 거절하라. 시험용 true boolean만으로 실제 승격이 가능하게 만들지 말라.

shadow 적격과 실제 활성화 권한을 분리하고 유효시각 이후 실행에만 선택된 버전을 반영하라. immutable snapshot 보존, 이중 승격 멱등성, risk latch 연속성, 열화 시 축소/중단과 적격 대안 복귀를 테스트하라.

실제 새 전략 활성화는 증거 조건을 모두 충족한 경우에만 가능하게 하라. 현재 증거가 부족하면 기능 검증은 fixtures로 완료하되 운영 Champion을 유지하고 자동 활성화는 차단된 상태로 남겨라. 미래 실측이 필요한 항목을 통과로 조작하지 말라.

이 단계는 자동 주문을 포함하지 않는다. 비교·결정 보고서와 감사 가능한 상태 이력을 완성하고, 운영 전환 계약의 독립 검수 Q로 인계하라.
```

## R6-A — 운영 안정성 및 실체결 관찰 준비

**추천: GPT-5.6 Sol / High.** R0/R1 이후 시작 가능하며 자동 연구 전체 완성을 기다리지 않는다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R6-A를 수행하라. 실제 주문은 실행하지 말라.

현재 자동 실행 기록으로 예정 실행·누락·지연·실패·stale/중복 여부를 점검하고, 사전 등록한 연속 20 XNYS sessions 운영 확인 및 이후 60 sessions 관찰을 구분하라. 경과하지 않은 기간을 simulated run으로 채우지 말라.

이미 구현된 기능은 재사용하며 필요한 최소 장애 감지·재개·백업 복원 확인을 추가하라. 강제 종료/네트워크 실패/lock 경쟁/restore 테스트는 격리된 환경에서 수행하고 live DB를 파괴하거나 덮지 말라. silent failure와 오래된 보고서의 오늘 재전송을 방지하라.

사용 가능한 체결내역 형식이 있으면 주문 권한 없는 read-only import로 실제 fills와 paper fills를 분리하라. 형식/자료가 없으면 schema와 fixture까지만 준비하고 실제 수익은 미확인으로 남겨라. 새 broker 접속이나 주문 권한을 임의로 추가하지 말라.

실전 검토에 필요한 자본·종목/총 노출·최대 손실·집행 조건의 필수 입력과 중단 동작을 마련하라. 실제 사용자 예산을 가정하지 말고, 누락이면 활성화만 막은 채 독립적인 구현은 완료하라.

운영 실측, 테스트로만 확인한 부분, 미래 관찰 대기를 구분하고 R6-B 판정에 필요한 자료 목록을 남겨라.
```

## R6-B — 소액 실전 검토 판정

**추천: GPT-6 Astra / Extra High.** 매수 실행 요청이 아니라 증거 검토 요청이다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 R6-B를 수행하라. 코드를 수정하거나 주문을 실행하지 말고 제한적 소액 실전 활용을 검토할 증거가 충분한지 판정하라.

동결 전략의 미사용 확인 평가, fresh shadow/paper, 운영 실측, outcome/자본 회계 완전성, 비용·갭·미체결 stress, 동일 자본·현금 조건 SPY 비교, tail/MDD와 집중도, 표본·신뢰구간을 대조하라. cohort drawdown과 실계좌 MDD를 구분하라.

R5 전체 자동화 미완성만으로 검토를 무조건 미루지는 말되 동일한 증거 수준을 우회하지 말라. 계획된 future gate와 실제 구현 정책이 다르면 차이를 드러내고 무단으로 낮은 쪽을 선택하지 말라.

사용자의 명시적 실전 자본/최대 허용손실/노출 한도와 실행 선택이 없다면 임의 설정하지 말고 빠진 필수 입력을 적어라. 개인별 법률·세금·거래 규정 등을 판단해야 할 경우 최신 공식 근거를 확인하라.

판정은 검토 가능 / 추가 증거 필요 / 현재 부적합 중 하나로 하고 근거와 중단 조건, 필요한 최소 다음 행동을 제시하라. 검토 가능은 수익 보장이나 주문 권한이 아니다. 증거 부족이면 paper 또는 NO TRADE를 유지하라. 사용자에게 현재 활용 가능한 범위와 가장 큰 실패 가능성을 짧게 설명하라.
```

## Q — 중요한 변경의 독립 검수

**추천: GPT-6 Astra / High.** 새 세션에서 실행하면 구현자의 결론에 덜 의존한다. 매번 별도 리뷰 문서를 만들 필요는 없다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 최근 완료했다고 보고한 작업을 독립 검수하라. PROJECT_STATUS와 Git diff/commit을 대조하여 검수할 단계와 변경 범위를 먼저 명시하라. 범위가 여러 단계이면 마지막 구현 단계와 직접 의존하는 변경을 대상으로 하라.

코드를 수정하지 말고 실제 계약·코드·테스트·산출물로 완료 주장을 확인하라. 중요한 항목은 미래 leakage, immutable 보존, feature/outcome 배당 구분, 누락/제외 분모, 계좌 회계, 연구/운영 격리, 모델 교체와 위험 latch, OOS 재사용 및 과장된 완료 판정이다.

필요한 테스트/읽기 전용 재현만 수행하고 원본 운영 DB/보고서를 변경하지 말라. 새로운 위험 근거 없이 같은 전체 테스트를 반복하지 말라.

finding마다 심각도, 파일/위치, 재현 조건, 사용자 영향, 최소 수정 방향을 제시하라. 새 기능 아이디어는 blocker로 부풀리지 말라. PASS / PASS WITH LIMITATIONS / CHANGES REQUIRED를 판정하고 개발 완료·운영 확인·미래 증거 대기를 구분하라.

문제가 없으면 다음 요청 ID를 추천하고, 문제가 있으면 F가 바로 처리할 finding 목록을 남겨라. 검수 보고서만으로 실제 투자 알파가 입증됐다고 쓰지 말라.
```

## F — 검수 지적사항만 수정

**추천: GPT-5.6 Sol / High.** finding이 없으면 사용하지 않는다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하고 가장 최근 검수의 미해결 finding만 수정하라. 검수 결과의 위치와 대상 변경을 먼저 확인하고 자료가 없으면 finding을 추측하지 말라.

각 finding의 원인을 확인한 뒤 최소 수정과 재발 방지 테스트를 추가하라. 검수에서 요청하지 않은 전략 개선·기능 확대는 하지 말라. 기존 사용자 변경과 immutable 운영 자료를 보존하라.

관련 테스트와 요구된 전체 검증을 수행하고 finding별 해결/미해결 및 재현 근거를 보고하라. 수정으로 핵심 계약이 바뀌면 이를 명시하고 Q의 해당 항목 재검수로 인계하라. 미래 증거 부족은 코드 수정으로 해결했다고 표시하지 말라.
```

## O — 자동 운영 중 주기적 상태 확인

**추천: GPT-5.6 Terra / Medium.** 매일 수동 요청할 필요는 없다. 주기적 검토 또는 장애 확인 때 사용한다.

```text
C:\richping에서 user_scripts.md의 공통 실행 규칙을 적용하여 현재 운영과 증거 축적 상태를 읽기 전용으로 확인하라. 이번 요청은 코드·설정·전략·스케줄러 변경이나 새 실험 실행 요청이 아니다.

마지막 점검 이후 예정 실행/시도/성공/실패/지연, 최신 dataset/report 시각, NO TRADE 이유, PENDING 만기와 UNRESOLVED/평가 제외, 모델별 fresh shadow 날짜·표본을 확인하라. paper가 구현됐으면 현금/노출/NAV 상태와 미확정 회계도 확인하라.

정상 동작과 실제 장애, 통계 표본 대기, 데이터 계약 blocker를 구분하라. 매일 OOS를 다시 열어 전략을 튜닝하지 말고 미사용 확인 구간은 사전 평가 시점 전까지 열지 말라. 미래 관찰 기간을 늘렸다는 이유만으로 충분한 표본이라고 판단하지 말라.

사용자에게 오늘 알아야 할 내용, 직접 조치가 필요한 예외, 다음 구현/관찰 작업 하나를 짧게 알려라. 원인이 확인되지 않은 실패는 가설로 표시하고 별도 수정이 필요한 범위만 제안하라.
```

## 권장 실행 흐름

```text
지금: R0-A → R0-B → R1-A → R1-B → 핵심 회계 검수 Q
운영: 실제 forward 자동 관찰 지속, 필요할 때 O
연구: R3-A → R3-B (좁은 범위), R2 후 넓은 범위로 확장
환경: R4-A가 GO인 경우에만 R4-B
개선: R5-A → R5-B → Q, 실제 증거 없으면 활성화 대기
실전: R6-A → 증거와 사용자 위험 입력 확보 후 R6-B
오류: Q에서 실제 finding이 있을 때 F → 해당 항목 재검수
```

R0/R1부터 보고서와 모의매매 효용을 얻는다. R2~R5 전체 완료가 소액 실전 검토의 무조건적 선행조건은 아니지만, R6의 증거·운영·위험 기준은 생략할 수 없다. 시간이 필요한 관찰은 계속 진행하고, 그동안 허용된 다른 개발을 수행한다.
